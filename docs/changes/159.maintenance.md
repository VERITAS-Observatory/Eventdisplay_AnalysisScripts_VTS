- Centralized machine-learning training parameter-space selection in `IRF.production.sh` and passed the selected zenith, NSB, and wobble grids into the TMVA and XGB gamma/hadron training scripts.
- Optimized IRF production startup by resolving the expensive simulation-derived parameter space only for workflows that actually require the full analysis grid.
